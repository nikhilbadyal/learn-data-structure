#include<stdlib.h>
#include<stdexcept>
#include<stdio.h>
#include"stackAdtLinked.h"
int main()
{
    int i ;
    Stack *s = nullptr;
    for( i = 0 ; i<5; ++i)
    {
    //    push(&s,3);
    }
    // push(s,4);
   display(s);
  // push(s,3);
   printf("%d ",peek(s));
    return 0;
}
